var chai = require('chai'),
    expect = chai.expect,
    combat = require('../index').combat;

describe('Combat API', function() {
    this.slow(75);

    it('can calculate combat level');

    it('accepts result from Hiscore API as an argument');
});